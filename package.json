{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap-ci": "npm run bootstrap -- --ci",
    "bootstrap": "lerna bootstrap --hoist",
    "lint": "lerna run lint",
    "publish": "lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
